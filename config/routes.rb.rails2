ChoreChopper::Application.routes.draw do
  resources :families do
    resources :chores
    resources :family_members
  end

  match 'account' => 'family'
  resource :family_session
  match '/' => 'home#index'
  match '/:controller(/:action(/:id))'
end
